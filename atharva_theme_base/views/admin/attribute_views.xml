<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Color Variant Image Form View -->
    <record id="product_template_attribute_value_form_extend" model="ir.ui.view">
        <field name="name">product.template.attribute.value.view.form.</field>
        <field name="model">product.template.attribute.value</field>
        <field name="inherit_id" ref="product.product_template_attribute_value_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[last()]" position="after">
                <field name="pv_thumbnail" invisible="display_type != 'color'" widget="image" options="{'size': [50, 50]}"/>
            </xpath>
        </field>
    </record>

    <!-- Color Variant Image list View -->
    <record id="product_template_attribute_value_list_extend" model="ir.ui.view">
        <field name="name">product.template.attribute.value.view.list</field>
        <field name="model">product.template.attribute.value</field>
        <field name="inherit_id" ref="product.product_template_attribute_value_view_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="before">
                <field name="pv_thumbnail"  invisible="display_type != 'color'" widget="image" optional="show"
                options="{'size': [50, 50]}"/>
            </xpath>
        </field>
    </record>
</odoo>